# Frontend environment variables
# Copy to .env and fill in values

# Backend API URL (on VPS: use your domain or server IP)
VITE_API_BASE=http://localhost:8000

# Upstream management panel URL
VITE_UPSTREAM_BASE=https://your-upstream-domain.com

# Upstream session cookies (from browser DevTools > Application > Cookies)
# Update when session expires
VITE_UPSTREAM_COOKIE=""
